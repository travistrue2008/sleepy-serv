name: Build
on: pull_request
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.3
      - name: Navigate to /lib Directory
        run: cd lib
      - name: Introspect Directory
        run: ls -a
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Test
        run: bun test
